Задание:
Написать функцию  bbsearch(f, df, x0, tol), реализующую поиск методом Барзилая-Борвейна 1, стабилизированным с параметром стабилизации D = 0.1. 

Входные переменные
• x0 — стартовая точка,
• tol — заданная точность.

Целевая функция и ее градиент заданы аналитически, это функция Розенброка.

Выходная переменная
• answer_ = [xmin, fmin, neval, coords]

где:
• xmin —  точка минимума, 
• fmin —  значение целевой функции в точке минимума,
• neval —  число вычислений производной целевой функции,
• coords —  массив координат x во время оптимизации.
Целевая функция и ее градиент заданы аналитически, это функция Розенброка.

Пояснение
В качестве критерия продолжения поиска используйте
    (norm(deltaX) >= tol) and (k < kmax),
где:
• kmax = 1000 — максимальное число итераций, 
• deltaX —  разница между двумя точками, найденными на соседних итерациях.

Для формирования массива coords воспользуйтесь конструкцией
    coords.append(x)

Для выбора начального шага воспользуйтесь методом золотого сечения для поиска по антиградиенту с интервалом изменения параметра [0, 1].